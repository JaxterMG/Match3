using Microsoft.Xna.Framework;

namespace Core.Cells
{
    public class GreenCellElement : CellElement
    {
        public GreenCellElement(CellType cellType, Color color, int x, int y, int size, Vector2 screenPos) : base(cellType, color, x, y, size, screenPos)
        {
        }
    }
}
